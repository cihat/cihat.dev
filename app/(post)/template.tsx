/**
 * Template component for blog posts
 * Now simplified - post metadata is handled within each MDX file
 */
export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
